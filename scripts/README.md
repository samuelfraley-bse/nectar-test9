# Scripts

Reusable Python modules imported by notebooks.

## Naming convention

- `load_data.py`   — data loading helpers
- `clean_data.py`  — cleaning & transformation
- `models.py`      — model definitions
- `utils.py`       — shared utilities
